let novaLista = []
let i = 0
while (i < Array.length) {
    let novoItem = "O elemento " + i.toString() + "é " novoItem += array[i].toString()
    novaLista.push(novoItem)
    i++
}
novaLista(1)
//2

for (let i = 0; i < array.length; i++ ){
    if (array.indexOf( array[i] ) === i ){
        models.push(array[ i ] );
    }
} 